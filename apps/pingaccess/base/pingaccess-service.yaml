apiVersion: v1
kind: Service

metadata:
  name: pingaccess

spec:
  selector:
    app: pingaccess

  ports:
    - name: https
      protocol: TCP
      port: 443
      targetPort: 9000

  type: ClusterIP
